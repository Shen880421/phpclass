<!--導航列-->
<style>
	.nav-cart-div {
		min-width: 480px;
	}
	.cart-li > div.nav-cart-div {
		display: none;
	}
	.cart-li:hover > div.nav-cart-div {
		display: block;
	}
</style>

<nav class="navbar navbar-expand-lg nav_top">
	<div
		class="container-fluid">
		<!-- 網站品牌 -->
		<a class="navbar-brand" href="#">電商後台
		</a>

		<!-- 漢堡選單按鈕 (小螢幕顯示) -->
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<!-- 導航菜單內容 -->
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="dashboard.php">後台首頁</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="logout.php">{{useracc}}</a>
				</li>
				<li class="nav-item me-3 cart-li position-relative">
					<a class="nav-link" aria-current="page" href="javascript:;">
						<i class="bi bi-cart4"></i>
					</a>
					<div class="z-3 position-absolute nav-cart-div">
						<div class="card shadow-sm">
							<div class="card-body">
								<table class="table table-striped table-hover">
									<tbody>
										{% for item in items %}
											<tr>
												<td>
													{% if item.cover %}
														<img src="uploads/{{item.cover}}" height="30">
													{% else %}
														<span>該產品暫無圖片</span>
													{% endif %}
												</td>
												<td>{{ item.name }}</td>
												<td class="text-end">{{ item.quan }}</td>
												<td>
													<a href="bk-product.php?mode=additem&pid={{item.id}}&quan=1" class="btn btn-sm btn-secondary" title="新增該品項">
														<i class="bi bi-plus-lg"></i>
													</a>
													<a href="bk-product.php?mode=reduceitem&pid={{item.id}}&quan=1" class="btn btn-sm btn-secondary" title="減少該品項">
														<i class="bi bi-dash-lg"></i>
													</a>
													<a href="bk-product.php?mode=removeitem&pid={{item.id}}" class="btn btn-sm btn-danger" title="移除該品項">
														<i class="bi bi-trash"></i>
													</a>
												</td>
											</tr>
										</a>
									</tbody>
								</td>
							</tr>
						{% endfor %}
					</tbody>
				</li>
			</table>
			<a href="ecpay.php" class="btn btn-primary">結帳</a>
		</div>
	</div>
</div></li><li class="nav-item">
<a class=" btn btn-outline-danger" aria-current="page" href="logout.php">登出</a></li></ul></div></div></nav>
